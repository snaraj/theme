# Copy this session file into a project, then run: kitty --session <file>
# Relative directories are resolved beside the session file.
# Existing Kitty configuration and directional split mappings still apply.
cd .
layout splits
launch --title Editor nvim
launch --location=vsplit --title Shell
launch --location=hsplit --title Output
